[package]
name = "rx_observer_examples"
edition = "2024"

[dependencies]
rx_observer = { path = "../rx-observer" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
parking_lot = "0.12"
chrono = { version = "0.4", features = ["serde"] }
xlformula_engine = "0.1"
